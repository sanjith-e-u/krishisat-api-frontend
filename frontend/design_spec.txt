<USER_REQUEST>
# Port full X-AGI design system from Lovable spec to this Next.js project

This is a complete visual system port — colors, typography, spacing,
backgrounds, animations, and interactive effects. This supersedes any
earlier "flat/minimal" design instructions you were given previously
— this spec is the new source of truth, full scope, nothing simplified
or skipped.

Implement in the phases below, IN ORDER. After each phase, run the
build and confirm no errors before moving to the next phase. Do not
attempt all phases in a single pass — verify incrementally.

## Phase 0 — Audit before building

Before writing any code:
1. Check package.json for: framer-motion, lucide-react, any existing
   theme/dark-mode provider (next-themes or custom). Report what
   exists vs. what needs installing.
2. Check if Geist/Geist Mono are available via next/font/google in
   the installed Next.js version. If not, report back and we'll
   decide on Inter + JetBrains Mono as fallback.
3. Check globals.css for any existing CSS custom properties or theme
   tokens already defined, so we extend rather than duplicate.

Report findings before proceeding to Phase 1.

## Phase 1 — Typography and color tokens

Add font setup:
```
--font-sans: "Geist", "Inter", ui-sans-serif, system-ui, sans-serif
--font-mono: "Geist Mono", "JetBrains Mono", ui-monospace, monospace
```
Apply globally on <html>:
```css
font-feature-settings: "cv11", "ss01", "ss03";
-webkit-font-smoothing: antialiased;
text-rendering: optimizeLegibility;
```

Type scale (implement as Tailwind classes or CSS custom properties,
whichever matches this project's existing convention):
```
H1 hero:      5rem/6rem/7rem (mobile/md/lg), weight 600, tracking-tight, leading-[1.05]
H2 section:   3rem → 4rem → 5rem, weight 600, tracking-tight
H3 card:      1.25rem → 2rem, weight 600
Body:         1rem / 0.875rem, weight 400, leading-relaxed
Eyebrow:      0.75rem mono, uppercase, tracking-widest, weight 400
Stats numb
<truncated 8282 bytes>
 seamless loop,
   telemetry tags: NDVI/NDMI/NDRE/SAVI/LST/RAIN/GDD/EVI/NDWI/CI/WIND/HUMID),
   CodePanel (gradient-ring border, macOS-style dots, LIVE badge with
   pulse, scan-line sweep, request/response split with terminal-
   typing effect on response side at 420cps with blinking cursor).

3. **Core APIs grid**: 3-col, gap-px bg-border, Tilt-wrapped cards
   with shine-host + tilt-glow.

4. **Unified Intelligence Layer**: bg-subtle section, 2-col grid,
   shine-host hover-glow cards.

5. **AI Models teaser**: gradient-ring + hover-glow + shine-host,
   "Coming Soon" badge, locked/disabled CTA.

6. **How It Works**: 3-col gap-px bg-border, large mono step numbers
   at accent/30 opacity, shine-host + bottom accent line on hover.

7. **Sandbox**: 2-col layout, checklist with accent checkmarks, tabs
   with bottom border indicator, mock auth box, Send button with
   simulated 600ms latency, scrollable monospace response panel.

8. **Pricing**: 3-col, popular tier border-accent + floating badge,
   accent Check icons, btn-magnetic full-width CTAs.

9. **Trust badges**: 3-col gap-px divider grid, 40×40px icon
   containers.

10. **Footer**: bg-subtle, 5-col grid, mono uppercase column titles,
    hover:text-accent links.

Build component by component, confirm build passes after each one,
before moving to the next.

## Final verification

1. Full build passes with zero errors
2. Toggle light/dark mode, confirm both look correct and the choice
   persists on reload
3. Confirm prefers-reduced-motion correctly disables all animation
4. Screenshot the hero section in both themes
5. Confirm the live ticker marquee loops seamlessly with no visible
   jump/snap at the loop point
6. Confirm 3D tilt and mouse spotlight effects only activate on
   devices with a mouse (not on touch — these should not error or
   feel broken on mobile/tablet, gracefully do nothing)
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-23T10:23:32+05:30.
</ADDITIONAL_METADATA>
---
